name: Release CORE-Gateway

on:
  push:
    tags:
      - 'v*.*.*'

jobs:
  # ===========================================================================
  # 1. Analyse Statique & Style (Flake8 & Black)
  # ===========================================================================
  lint:
    name: Lint Code
    runs-on: ubuntu-latest
    outputs:
      warnings_count: ${{ steps.parse.outputs.warnings_count }}
      formatted: ${{ steps.parse.outputs.formatted }}
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4
      
      - name: Set up Python
        uses: actions/setup-python@0b93645e9fea7318ecaed2b359559ac225c90a2b # v5
        with:
          python-version: '3.10'
          cache: 'pip'
          
      - name: Install linting tools
        run: |
          pip install flake8 black
          
      - name: Run flake8 Analysis
        run: |
          # Bloque immédiatement le workflow s'il y a des erreurs de syntaxe critiques
          flake8 . --count --select=E9,F63,F7,F82 --show-source --statistics
          # Compte les autres avertissements de style et de complexité
          flake8 . --count --exit-zero --max-complexity=10 --max-line-length=127 --statistics > flake8_report.txt

      - name: Check code formatting (Black)
        run: |
          black --check . --exclude='(\.git|\.github|build|dist)' > black_report.txt 2>&1 || true

      - name: Parse Reports
        id: parse
        run: |
          # Extrait le nombre d'avertissements de flake8
          WARNINGS=$(tail -n 1 flake8_report.txt || echo "0")
          echo "warnings_count=$WARNINGS" >> $GITHUB_OUTPUT
          
          # Vérifie si Black demande un reformatage
          if grep -q "would reformat" black_report.txt; then
            echo "formatted=no" >> $GITHUB_OUTPUT
          else
            echo "formatted=yes" >> $GITHUB_OUTPUT
          fi

      - name: Upload Lint Reports
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        with:
          name: lint-reports
          path: |
            flake8_report.txt
            black_report.txt

  # ===========================================================================
  # 2. Détection de Duplication de Code (jscpd)
  # ===========================================================================
  duplication:
    name: Code Duplication (jscpd)
    runs-on: ubuntu-latest
    outputs:
      percentage: ${{ steps.parse.outputs.percentage }}
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4
      
      - uses: actions/setup-node@39370e3970a6d050c480ffad4ff0ed4d3fdee5af # v4.1.0
        with:
          node-version: "20"
          
      - name: Install jscpd
        run: npm install -g jscpd
        
      - name: Analyse des duplications
        run: |
          jscpd . \
            --threshold 10 \
            --min-lines 5 \
            --min-tokens 50 \
            --reporters "console" \
            --ignore "**/__pycache__/**,**/dist/**,**/build/**,**/.git/**,**/node_modules/**,**/*.wav,**/*.exe,**/*.spec,**/*.bak,**/face-server/static/**,**/face-server/templates/**" > jscpd_output.txt 2>&1 || true
            
      - name: Parse and validate duplication
        id: parse
        run: |
          python -c "
          import re, os
          with open('jscpd_output.txt') as f:
              text = f.read()
          match = re.search(r'(\d+\.?\d*)\s*%', text)
          val = float(match.group(1)) if match else 0.0
          percentage = f'{val}%'
          print(f'Duplication détectée: {percentage}')
          with open(os.environ['GITHUB_OUTPUT'], 'a') as out:
              out.write(f'percentage={percentage}\n')
          if val > 10.0:
              print('Error: La duplication de code dépasse le seuil maximal autorisé de 10% !')
              exit(1)
          "

  # ===========================================================================
  # 3. Analyse de Sécurité SAST (Semgrep)
  # ===========================================================================
  security:
    name: SAST Security Scan (Semgrep) [info]
    runs-on: ubuntu-latest
    continue-on-error: true
    outputs:
      findings_count: ${{ steps.parse.outputs.findings_count }}
    container:
      image: semgrep/semgrep:1.114.0
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4
      
      - name: Scan Semgrep
        env:
          SEMGREP_SEND_METRICS: "off"
        run: |
          semgrep scan --config p/security-audit --config p/owasp-top-ten --json --output semgrep_results.json . || true
          
      - name: Parse security findings (non-blocking)
        id: parse
        run: |
          python -c "
          import json, os
          with open('semgrep_results.json') as f:
              data = json.load(f)
          count = len(data.get('results', []))
          print(f'[INFO] Failles Semgrep trouvées: {count} (non-bloquant)')
          with open(os.environ['GITHUB_OUTPUT'], 'a') as out:
              out.write(f'findings_count={count}\n')
          "

  # ===========================================================================
  # 4. Scan de Vulnérabilités & Secrets (Trivy)
  # ===========================================================================
  vulnerability:
    name: Vuln & Secrets Scan (Trivy) [info]
    runs-on: ubuntu-latest
    continue-on-error: true
    outputs:
      issues_count: ${{ steps.parse.outputs.issues_count }}
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4
      
      - name: Scan Trivy
        uses: aquasecurity/trivy-action@ed142fd0673e97e23eac54620cfb913e5ce36c25 # v0.36.0
        with:
          scan-type: "fs"
          scan-ref: "."
          scanners: "vuln,secret"
          severity: "HIGH,CRITICAL"
          format: "json"
          output: "trivy_results.json"
          
      - name: Parse Trivy findings (non-blocking)
        id: parse
        run: |
          python -c "
          import json, os
          with open('trivy_results.json') as f:
              data = json.load(f)
          results = data.get('Results', [])
          issues = 0
          for r in results:
              issues += len(r.get('Vulnerabilities', [])) + len(r.get('Secrets', []))
          print(f'[INFO] Vulnérabilités Trivy HIGH/CRITICAL: {issues} (non-bloquant)')
          with open(os.environ['GITHUB_OUTPUT'], 'a') as out:
              out.write(f'issues_count={issues}\n')
          "

  # ===========================================================================
  # 5. Création de l'Archive & Publication de la Release
  # ===========================================================================
  release:
    needs: [lint, duplication]
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4

      - name: Download Lint Reports
        uses: actions/download-artifact@d3f86a106a0bac45b974a628896c90dbdf5c8093 # v4.3.0
        with:
          name: lint-reports
          path: ./reports

      - name: Extract version
        id: version
        env:
          REF_NAME: ${{ github.ref_name }}
        run: echo "VERSION=$REF_NAME" >> $GITHUB_OUTPUT

      - name: Update version.txt
        env:
          VERSION: ${{ steps.version.outputs.VERSION }}
        run: echo "$VERSION" > face-server/version.txt

      - name: Create Release Archive
        env:
          VERSION: ${{ steps.version.outputs.VERSION }}
        run: |
          cd face-server
          zip -r ../core-gateway-$VERSION.zip . \
            --exclude "__pycache__/*" \
            --exclude "*.pyc"

      - name: Generate Release Body
        run: |
          cat << 'EOF' > release_body.md
          ## CORE-Gateway ${{ steps.version.outputs.VERSION }}

          ### 📦 Installation rapide
          ```bash
          # Télécharger et déployer
          curl -L https://github.com/Bot-Bastet/CORE-Gateway/releases/download/${{ steps.version.outputs.VERSION }}/core-gateway-${{ steps.version.outputs.VERSION }}.zip -o gateway.zip
          unzip gateway.zip
          cp .env.example .env  # Configurer le token
          docker compose up -d --build
          ```

          ### 🔄 Mise à jour depuis une installation existante
          Le service se met à jour automatiquement via l'auto-updater intégré.

          ### 📊 Quality Gate
          | Test | Statut | Métrique |
          | :--- | :---: | :---: |
          | 🔍 Syntaxe Flake8 | ✅ | 0 erreur critique |
          | 📏 Flake8 warnings | ℹ️ | ${{ needs.lint.outputs.warnings_count }} |
          | ✨ Formatage Black | ${{ needs.lint.outputs.formatted == 'yes' && '✅' || '⚠️' }} | ${{ needs.lint.outputs.formatted == 'yes' && 'OK' || 'Non formaté' }} |
          | 📏 Duplication jscpd | ✅ | ${{ needs.duplication.outputs.percentage }} (seuil 10%) |
          | 🛡️ Semgrep SAST | ⚠️ non-bloquant | Vérification de sécurité non activée pour cette release |
          | 🔐 Trivy vulns | ⚠️ non-bloquant | Vérification de vulnérabilités non activée pour cette release |
          EOF

      - name: Publish GitHub Release
        uses: softprops/action-gh-release@3bb12739c298aeb8a4eeaf626c5b8d85266b0e65 # v2.6.2
        with:
          name: "CORE-Gateway ${{ steps.version.outputs.VERSION }}"
          body_path: release_body.md
          files: |
            core-gateway-${{ steps.version.outputs.VERSION }}.zip
          draft: false
          prerelease: ${{ contains(steps.version.outputs.VERSION, '-beta') || contains(steps.version.outputs.VERSION, '-rc') }}
